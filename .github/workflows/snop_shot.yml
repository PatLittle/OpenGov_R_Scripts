name: CHAOSS Blog RSS Feed

on:
  push:
  workflow_dispatch:
  schedule:
    - cron: "0 0/8 * * *"

jobs:
  gene-rss:
    runs-on: ubuntu-latest
    name: A job to gene RSS
    steps:
    - name: Checkout
      uses: actions/checkout@v2
